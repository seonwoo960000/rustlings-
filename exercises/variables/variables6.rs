// variables6.rs
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a hint.

// constants are always immutable
// constants are always declared with a type annotation
const NUMBER: i32 = 3;
fn main() {
    println!("Number {}", NUMBER);
}
